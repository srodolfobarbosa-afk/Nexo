# Dependências principais (gerenciadas via pyproject.toml)

# Versões fixadas (se necessário, para deploy ou ambientes específicos)
fastapi==0.95.2
uvicorn[standard]==0.22.0
requests==2.31.0
PyGithub>=1.59.1,<2

