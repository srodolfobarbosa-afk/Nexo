[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nexo"
version = "0.1.0"
description = "Nexo - agente de integração e orquestração"
authors = [
  { name = "Nexo Maintainers" }
]
dependencies = []

[tool.ruff]
line-length = 88
select = ["E", "F", "W", "C", "Q"]
